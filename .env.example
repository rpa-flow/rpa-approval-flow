DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DB?sslmode=require"

# SMTP opcional (se não preencher, o envio é simulado via log)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE="false"
SMTP_FROM="notificacoes@empresa.com"

# Chave opcional para endpoint global de lembretes
REMINDER_API_KEY=""
