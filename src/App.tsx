export default function App() {
  return (
    <main className="mx-auto mt-10 max-w-4xl rounded-card border border-border bg-surface-container-lowest p-8 shadow-card">
      <h1 className="text-2xl font-semibold text-text">RPA Approval Flow</h1>
      <p className="mt-3 text-muted">
        Stack base preparada com <strong>React + Vite + TypeScript + Tailwind</strong>.
      </p>
      <p className="mt-2 text-sm text-muted">
        Próximo passo: migrar gradualmente as telas de Next.js para componentes/client routes no novo frontend.
      </p>
    </main>
  );
}
